import { Routes } from '@angular/router';

const appRoutes: Routes = [
  // hna fin kit7ato routes, ex: { path: 'home', component: HomeComponent }
  // hadik ght3ni bli ila l9at flpath matalan localhost/home, ghay afficher l HomeComponenet
];
export default appRoutes;
